# AI 工具定位表

> 核心原则：知道"什么工具解决什么问题"，而不是盲目追工具。

---

## 一、正在使用的工具

| 工具名称 | 主要用途 | 适合任务 | 我现在是否需要 | 我准备如何使用 | 可能的问题 |
|----------|----------|----------|---------------|---------------|-----------|
| **DeepSeek**（网页版） | 通用大语言模型：对话、推理、代码生成、文本理解 | 写 Node.js 脚本、PDF 内容结构化、数据校验规则编写、文档内容总结 | ✅ 日常刚需 | 作为主力"技术外脑"，遇到不会写的代码直接问，遇到需要理解的非结构化文本丢给它做结构化。近期准备从"网页版手动聊天"升级为"API 程序化调用"，用 Function Calling 让它自动输出 JSON 而非手动复制粘贴 | 1. 长对话下偶尔遗忘早期约束 2. 复杂嵌套表格识别需多次纠正 3. 生成代码偶有语法错误需实测 4. 网页版手动操作的效率上限低，不改 API 就无法进入自动化管线 |
| **Claude Code CLI** | AI 编程助手：多步骤任务编排、文件批量操作、代码编辑、Git 操作 | 搭建项目脚手架、跨文件代码修改、生成文档、管理 GitHub 仓库 | ✅ 日常刚需 | 作为命令行里的"工程助手"，用它做三类事：① 从零搭建项目结构（比如本仓库的全部文件）② 在已有代码中精准定位和修改逻辑 ③ 编排多步骤任务（写文档 → 建仓库 → 推送一气呵成）。后续准备探索 Dynamic Workflows 来编排文档处理管线 | 1. 偶尔理解偏差导致返工 2. Windows 路径处理有时需要手动纠正 3. 长会话后 token 消耗需要关注 4. API 在国内的连通性有时不稳定 |
| **ChatGPT** | 通用大语言模型：备选方案和交叉验证 | 当 DeepSeek 方案的可靠性不确定时，用 ChatGPT 做二次确认 | 🟡 偶尔需要 | 不做主力，定位是"第二意见"——当 DeepSeek 给出的代码或方案我怀疑有问题时，拿同样的问题问 ChatGPT，对比两个回答的一致性。如果两个模型给出的思路一致，可信度更高 | 1. 免费版容量有限 2. 与 DeepSeek 功能高度重叠，大多数时候有一个就够了 3. 没有形成固定的互补使用习惯 |

## 二、配合 AI 使用的文档工具

| 工具名称 | 主要用途 | 适合任务 | 我现在是否需要 | 我准备如何使用 | 可能的问题 |
|----------|----------|----------|---------------|---------------|-----------|
| **pdf-parse**（Node.js 库） | 从 PDF 文件中提取文本内容 | 文字型 PDF 的文本提取，作为 AI 处理的前置步骤 | ✅ 当前在用，但有替代计划 | 目前用于所有 PDF → 文本的第一步提取。后续计划：对于文字型 PDF 继续用，但对于扫描件和复杂表格，改用 MinerU 或 DeepSeek-OCR-2 替代 | 1. 扫描件 PDF 完全无能为力 2. 中文竖排/混合排版提取结果混乱 3. 表格还原度极低，提取后表格结构基本丢失 4. 后续可能需要替换，投入的时间可能成为沉没成本 |
| **docx**（Node.js 库） | 用代码生成 .docx 格式的 Word 文档 | AI 处理完的数据写入格式化 Word 报告 | ✅ 当前在用 | 在 Pipeline 的最后一步使用：AI 输出的结构化 JSON → docx 脚本 → 格式化的 Word 文件。后续需要建立常用模板（报告、数据简报、合同摘要），避免每次从零写样式 | 1. 样式 API 不够直观，调格式很花时间 2. 复杂表格（合并单元格、嵌套表格）代码量很大 3. 中文排版（首行缩进、行距）的设置需要反复调试 |

## 三、准备学习或尝试的工具

| 工具名称 | 主要用途 | 适合任务 | 我现在是否需要 | 我准备如何使用 | 可能的问题 |
|----------|----------|----------|---------------|---------------|-----------|
| **DeepSeek-OCR-2** | DeepSeek 的专用文档解析模型，视觉到文本提取 | 扫描件 PDF、图片型文档的文字识别和结构化提取 | 🔴 迫切——这填补了我当前工具链最大的缺口 | 替代"pdf-parse + 手动整理"的环节。直接输入 PDF 或图片，输出结构化文本。配合 DeepSeek V4 的 Function Calling，让文档提取这一步完全自动化——不需要手动打开网页版 | 1. 中文复杂排版（竖排、印章遮挡）的实际效果需实测 2. 作为 DeepSeek 生态的一部分，存在供应商锁定 3. 定价 $0.15/1M tokens，如果大量使用需要关注成本 |
| **MinerU** | 开源文档解析工具（VLM + OCR 双引擎） | 中文 PDF 的表格提取、公式识别、跨页表格合并 | 🟡 强烈感兴趣的备选方案 | 和 DeepSeek-OCR-2 做对比测试——同样的 PDF，两个工具分别提取，比准确率和效率。如果 MinerU 在中文表格上的表现更好，就作为本地主力方案 | 1. 主要 Python 生态，而我的技术栈是 Node.js，需要跨语言集成 2. 本地部署需要 GPU 资源，我的 Windows 环境可能跑不动 3. 开源项目维护节奏不确定 |
| **pdf-mcp** | MCP Server：让 AI Agent 精准读取 PDF 的特定页面 | 用语义搜索在长 PDF 中定位相关页面，只读需要的部分 | 🟡 强烈感兴趣的效率工具 | 集成到 Claude Code 工作流中：不再把整份 PDF 全文粘贴给 AI，而是让 AI 通过 pdf-mcp 自己搜索、定位、阅读需要的页面。大幅减少 token 消耗 | 1. 依赖 MCP 生态，需要先理解 MCP 的配置和使用 2. 网络环境下 MCP Server 的安装可能遇到问题 3. 目前 v1.19.1，还比较新，API 稳定性需关注 |
| **ParseHawk** | 本地优先的文档 AI 工具：用 JSON Schema 约束输出结构 | 从 PDF/扫描件中提取符合自定义 Schema 的结构化 JSON | 🟢 关注中，暂不急用 | 当我的 Pipeline 需要"按指定格式输出结构化数据"时，评估是否比直接调 DeepSeek API + Prompt 约束更可靠。优势是 Schema-constrained decoding，可以保证输出格式完全符合预期 | 1. 版本目前 v0.1.2，太早期了 2. 中文支持情况不明 3. 需要本地部署 vLLM，我的环境可能不满足要求 |

## 四、工具选择逻辑

```
                    ┌─────────────────────────┐
                    │      输入：文档/PDF       │
                    └───────────┬─────────────┘
                                │
              ┌─────────────────┼─────────────────┐
              ▼                  ▼                  ▼
       文字型 PDF          扫描件/图片型        格式混乱的 PDF
              │                  │                  │
              ▼                  ▼                  ▼
         pdf-parse        DeepSeek-OCR-2        MinerU
       （当前方案）        （准备切换）         （对比测试）
              │                  │                  │
              └──────────────────┼──────────────────┘
                                 │
                                 ▼
                    ┌─────────────────────────┐
                    │   AI 处理：DeepSeek API  │
                    │   Function Calling 模式  │
                    │   输出结构化 JSON         │
                    └───────────┬─────────────┘
                                │
                                ▼
                    ┌─────────────────────────┐
                    │   输出：docx 生成 Word    │
                    │   模板驱动 + 数据填充     │
                    └───────────┬─────────────┘
                                │
                                ▼
                    ┌─────────────────────────┐
                    │   全程编排：Claude Code   │
                    │   + Dynamic Workflows    │
                    └─────────────────────────┘
```

## 五、工具选择原则

1. **场景优先，不追新品**：不是因为某个工具热门就去学，而是因为"我的场景需要它解决什么问题"
2. **先用熟再换**：DeepSeek 和 Claude Code 已经深度使用，先挖透它们的能力（Function Calling、Workflows），再考虑引入新工具
3. **对比实测，不盲信宣传**：同一个文档，两个工具分别跑，看数据而不是看宣传
4. **控制工具数量**：当前 9 个工具（在用 5 个 + 准备学 4 个），后续该淘汰的淘汰（如 pdftotext 已弃用），保持工具链精简
